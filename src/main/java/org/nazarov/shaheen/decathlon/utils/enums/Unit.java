package org.nazarov.shaheen.decathlon.utils.enums;

import javax.xml.bind.annotation.XmlRootElement;

@XmlRootElement(name = "unit")
public enum Unit {
    CENTIMETERES,METRES,SECONDS,MINUTES_SECONDS;
}
